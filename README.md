# LOAN_PREDICTION_ML_MODEL
A classification project that predicts loan approval using data preprocessing, categorical encoding, train-test splitting, cross-validation, and a Random Forest Classifier.
